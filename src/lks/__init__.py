"""Legal Knowledge System (LKS).

Two engines, never blended:
  * CATALA  -- deterministic rules, answered by *executing* a scope.
  * VECTOR  -- non-deterministic prose, answered by *quoting* with citations.
"""
__version__ = "0.1.0"
